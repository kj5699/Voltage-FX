import '@store/index'
import '@pipelines/index'
import '@ws/index'
import '@hooks/index'
import '@config/index'
import '@utils/index'

describe('path aliases', () => {
  it('all aliases resolve without error', () => {
    expect(true).toBe(true)
  })
})
